{
    "version": "2.0.0",
    "configurations": [
        {
            "name": "Run C/C++",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Game",
            "cwd": "${cwd}",
            "externalConsole": true,
            "preLaunchTask": "build"
        }
    ]
}